package com.payguard.payment.repository; import com.payguard.payment.domain.Transaction; import java.util.*; import org.springframework.data.jpa.repository.JpaRepository; public interface TransactionRepository extends JpaRepository<Transaction,UUID>{ Optional<Transaction> findByTransactionIdAndMerchantId(String id,String merchant); Optional<Transaction> findByStripeChargeId(String id); List<Transaction> findByMerchantIdAndStatusOrderByCreatedAtAsc(String merchant,com.payguard.payment.domain.TransactionStatus status); }
